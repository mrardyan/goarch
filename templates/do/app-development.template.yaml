name: golang-arch-dev
services:
  - name: golang-arch
    source_dir: /
    github:
      repo: mrardyan/golang-arch
      branch: main
      deploy_on_push: true
    dockerfile_path: /Dockerfile
    http_port: 8080
    instance_count: 1
    instance_size_slug: apps-s-1vcpu-0.5gb
    health_check:
      http_path: /health/ping
    envs:
      # Environment variables will be added here by the generator script
      # from .env.development file

databases:
  - name: db
    engine: PG
    version: "15"
    production: true
    cluster_name: golang-arch-dev-cluster
    db_name: golang_arch_dev
    db_user: golang_arch

domains:
  - domain: api.dev.golang-arch.app
    type: PRIMARY
    zone: dev.golang-arch.app

alerts:
  - rule: DEPLOYMENT_FAILED
  - rule: DOMAIN_FAILED

features:
  - buildpack-stack=ubuntu-22

ingress:
  rules:
    - component:
        name: workluv
      match:
        path:
          prefix: /

region: sgp 